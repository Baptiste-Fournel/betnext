# URL de base de l'API back (NestJS). Le client typé pointe ici. (Pas de secret : URL publique.)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
# URL de l'app JOUEUR (sœur) — sert juste le lien « mauvaise interface » si un PLAYER se connecte ici.
NEXT_PUBLIC_PLAYER_URL=http://localhost:3001
