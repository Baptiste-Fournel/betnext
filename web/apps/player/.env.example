# URL de base de l'API back (NestJS). Le client typé pointe ici. (Pas de secret : URL publique.)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
# URL de l'app ADMIN (sœur) — sert juste le lien « mauvaise interface » si un MANAGER se connecte ici.
NEXT_PUBLIC_ADMIN_URL=http://localhost:3002
